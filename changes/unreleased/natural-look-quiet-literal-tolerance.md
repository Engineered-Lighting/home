---
title: Tolerate paraphrased quiet findings in natural look
target: desktop
type: fixed
---

The deep-look classifier now matches "no obvious activity" anywhere in a
finding instead of requiring the exact sentence, so a model that pads the
phrasing no longer turns every idle camera into a reported notable finding.
