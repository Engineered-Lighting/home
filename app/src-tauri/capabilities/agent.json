{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "agent",
  "description": "Minimal local capability for the isolated Home Agent window",
  "windows": ["agent"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
