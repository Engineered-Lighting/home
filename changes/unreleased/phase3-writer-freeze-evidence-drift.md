---
title: Bind Phase 3 Writer-Freeze Evidence to the Reviewed Snapshot
target: backend
type: fixed
---

The Phase 3 writer-freeze evidence compared the physical Home Assistant observation against the projection digest the private People review reported at review time, a value no signing verb re-derived; a reviewed identity-loader fix merged after the review therefore stalled the activation at step 21 with `physical source projection drifted`. The evidence now binds the observation to the projection of the reviewed snapshot itself, the signing verbs refuse a review whose projection has drifted when it is staged rather than six steps later, and step 21 re-measures the freeze and re-verifies the erasure receipt so a resumed run still lands inside the five-minute windows the privacy observer and the semantic cutover kernel enforce. Steps 21-35 of an in-flight run execute under the new source pin while the run's recorded tool-bundle digest and completion receipt continue to describe the pin in force at finalization; record the pin PR number beside the run id, because nothing reconciles the two automatically.
