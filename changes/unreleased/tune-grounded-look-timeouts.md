---
title: Tune grounded look timeouts
target: web
type: fixed
---

Deep visual lookups now wait long enough for local zoom and segmentation passes, and failed deep looks collapse into a concise fallback message instead of noisy per-camera timeout diagnostics.
