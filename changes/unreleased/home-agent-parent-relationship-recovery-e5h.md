---
title: Add recoverable private parent confirmation
target: web
type: added
---

Adds the dormant E5h private Agent review for recovering, previewing, and
atomically confirming the two reviewed parent relationships across browser
refreshes. PostgreSQL remains the authority, expired previews are closed under
the semantic write fence, and confirmed status exposes no parent names;
production remains pinned to record-only revision `0006a`, with location
memory and travel greetings still off.

Accepted by GitHub Actions PostgreSQL authority run `30388823143` and web
boundary run `30387665230`. The hosted gates passed the complete E1–E5h
database replay, table-blind status recovery, expired-preview closure,
content-minimized confirmed state, fresh Home Assistant reauthentication,
browser-only authority boundary, private UI encoding, and labeled cleanup.
