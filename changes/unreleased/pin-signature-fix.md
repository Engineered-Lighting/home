---
title: Pin the activation source to the migrate stop and signature fixes
target: internal
type: changed
---

The pin half for the entrypoint migration stop and the kernel signature fix.
Both changed pinned activation paths — `docker-entrypoint.sh` and the
migrations.
