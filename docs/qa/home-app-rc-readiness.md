# Home App RC Readiness Log

Generated by Codex during the release-candidate hardening loop.

## 2026-06-23 Local Non-Production Pass

Scope boundary:

- Used only local/static/unit/build checks.
- Did not run live HA, Frigate, voice, lighting, media, stack-control, or other device-affecting scenarios.
- Live/manual scenarios remain documented in `docs/SCENARIO-TESTS.md` and registered in `tools/qa-features.yaml`, but are out of scope for this non-production pass.

Current feature inventory from `tools/qa-registry-audit.py`:

- 226 canonical QA entries.
- 99 documented scenarios.
- 17 live agent scenarios.
- 18 screenshot recipes.
- 22 feature probes.
- 70 quick/unit suites.
- 51 slash commands.
- 62 simulation scenarios.
- 48 frontend scripts.
- 113 first-party FastAPI routes.
- 17 Home Assistant views.
- 24 Home Assistant packages.
- 10 Docker Compose services.
- 14 major UI surfaces.
- 8 Living Lights classifier states.
- 50 Home Assistant input_boolean flags.
- 23 Extended OpenAI agent tools.
- 4 spatial model cameras.
- 16 spatial model lights.
- 5 predictive-lighting MQTT publish topic families.
- 3 predictive-lighting MQTT subscription topic families.
- 12 stack-supervisor endpoints.
- 4 architecture decisions in the consolidated ADR trail.
- Scenario evidence map: 6 documented scenarios simulation-covered, 67 local-contract-covered, 26 locally partial, and 0 live-required with no sufficient local substitute. Local-partial scenarios still require live/non-production validation for full end-to-end proof.
- Local-partial scenario IDs: DOC-S13, DOC-S19, DOC-S35, DOC-S58 through DOC-S74, DOC-S105 through DOC-S110.

Checks run:

- `py -3 tools\run-test-coverage.py --quick --json`: pass in 3 consecutive runs, 70 suites, 3,824 assertions.
- `py -3 tools\run-test-coverage.py --json`: pass, 70 suites, 3,824 assertions. The current registry has 0 slow suites, so quick and full local coverage cover the same set.
- `node tools\check-jsx.js home-app.jsx home-worldstate.jsx home-lights.jsx home-ai-stack.jsx`: pass, 4 files.
- `py -3 tools\qa-static-checks.py`: pass, 21 checks.
- `py -3 tools\qa-registry-audit.py`: pass, 0 warnings.
- `node tools\qa-browser-smoke.js` with no prestarted dev server: pass, 21 checks. The Stage 4 smoke self-hosted `app/src` on a loopback ephemeral port, verified critical cache-busted boot modules are served as JavaScript, and wrote `stage4-browser-smoke.json`.
- In-app browser smoke at `http://127.0.0.1:5180/?simulation=1&scenario=model-offline`: pass. `#root` populated, no boot overlay, no console errors, `vllm missing` was rendered, and the `start ai stack` recovery action was visible.
- `cargo check` in `app/src-tauri`: pass.
- `cargo test` in `app/src-tauri`: pass, 5 Rust unit tests.
- `cargo tauri build --no-bundle` with isolated `CARGO_TARGET_DIR=target-rc`: pass.
- `cargo tauri build` with isolated `CARGO_TARGET_DIR=target-rc-bundle`: pass, produced `app/src-tauri/target-rc-bundle/release/bundle/msi/Home_0.1.0_x64_en-US.msi`.
- `msiexec /a app/src-tauri/target-rc-bundle/release/bundle/msi/Home_0.1.0_x64_en-US.msi /qn TARGETDIR=tmp/tauri-msi-admin-20260623-171634`: pass, extracted `PFiles/Home/home.exe` without installing system-wide.

Bug fixed in this pass:

- Static QA and registry route discovery walked into `stack/services/video-labeler/.venv/Lib/site-packages`, so FastAPI's vendored example routes were counted as product routes and falsely failed duplicate-route checks.
- Fixed by adding a first-party source filter to `tools/qa-static-checks.py` and `tools/qa-registry-audit.py`.
- Added a regression check proving virtualenv/vendor paths are excluded while first-party service files remain included.
- The Home app exposed Lab/AI stack action methods for `free gpu`, per-service logs, per-service stop, and per-service restart, but the stack supervisor only implemented whole-stack start/restart/stop/status routes.
- Fixed by adding allowlisted supervisor routes for `/api/services/{service}/logs`, `/api/services/{service}/stop`, `/api/services/{service}/restart`, and `/api/stack/free-gpu` (plus `/api/stack/free_gpu` compatibility). Every non-`/healthz` route requires bearer auth; destructive stop/free-GPU routes also require explicit confirmation headers; and service names resolve through a fixed compose-service map.
- Several UI paths called `client.callService(...)`, but `HAClient` only exposed the lower-level websocket `call(payload)` method. That made service-backed controls fragile and left `/lights` sleep/night-safe toggles surfacing raw disconnected-state errors.
- Fixed by adding `HAClient.callService(...)`, centralizing `call_service` payload shaping with `target` vs `service_data`, threading HA connection state into the `/lights` drawer, gating writes while HA is reconnecting, and subscribing the drawer directly to `input_boolean.homeai_sleep`.
- The `/lights` drawer still left numeric sliders, bias-scope buttons, footer clear/reset actions, and its manual-cooldown sweeper visually/actionably live while HA was disconnected, so a user could make changes that looked accepted but could not stick.
- Fixed by disabling every rendered drawer slider plus the scope/footer write buttons while HA is offline, keeping the existing offline error message on write helpers, and preventing the cooldown sweeper from issuing service calls until HA is online again.
- `/lab-dump-watch` read `parts[1]` even though that command branch has no `parts` variable, so the timing-window evidence command could throw before starting or stopping its cadence timer.
- Fixed `/lab-dump-watch` to parse the actual slash-command argument string, and made `/route-log` visible in help/autocomplete while keeping `/routes` as its handler alias.
- The external-reasoning self-test reported "no key configured" before "Simulation Mode active", even though Simulation Mode is the stronger safety condition and should clearly explain why no live provider call will run.
- Fixed the external suite runner to prioritize the Simulation Mode live-call skip before checking for a configured external API key.
- The S96-S99 external-provider scenarios had weak local contracts: `/ask` and auto-route only checked `openaiProvider` even though `askExternal` supports a custom active provider, `/external status` omitted last-call time/token details, and the frontend prompt did not explicitly require plain prose/no markdown.
- Fixed by adding active-provider helpers, a status formatter with last-call time/age/latency/token stats, success-stat recording for custom providers, S96 plain-prose prompt constraints, HomeApp command wiring to the active provider, and matching HA voice prompt/test coverage for GENERAL questions and provider failures.
- The S105-S110 outage/recovery scenarios had scattered local evidence, so it was too easy to miss whether HA/vision/Frigate/Sonos/metrics/bridge failures degraded visibly and recovered stale state instead of silently disappearing.
- Fixed by adding `tools/run-recovery-scenarios-tests.js`, a pure local suite covering HA offline UI/write-disable behavior, vision-sidecar fallback copy and explicit errors, Frigate stale-camera/perception-offline mapping, Sonos unavailable/call-failure surfacing, metrics stale-data clearing, bridge/voice fallback, S2S chat errors, and SSE error/close callbacks.
- The local model could still emit `execute_services` with `light.turn_on` for managed-room brightness/color requests even though the prompt says to use `set_presence_override`; that produces the exact failure mode where the lights jump to the requested level, then Living Lights treats the service call as non-durable noise and reverts it.
- Fixed by adding a narrow backend reroute in `functions/native.py`: managed-room `light.turn_on` calls with brightness or white-temperature data now call `set_presence_override` instead, while `turn_off`, unmanaged lights, and unsupported color modes still dispatch normally. `test_native.py` now covers kitchen area, virtual kitchen light, dining kelvin, and pass-through cases.
- HomeKit/Siri/Lutron or broad HA service calls can report the user touch as a room/group entity such as `light.kitchen`, `light.kitchen_lights`, `light.dining_room`, `light.dining_room_lights`, or `light.living_room_lights`; those aggregate entities were not in the fast manual-hold armer list, so the physical-light armers could miss the race and the pilots could dim the room back down.
- Fixed by adding aggregate room/group fast armers in `build-living-lights-actuators.py` and regenerating `living_lights_manual_detection.yaml`. Aggregate armers stamp the same room-cover `last_manual_at` and context helpers immediately but do not create settled preference-learning events; physical lights still own the 3-second settled detector.
- `/look` accepted camera names and ids but not pasted Home Assistant camera entity ids like `camera.kitchen`, so a natural copied entity id was treated as part of the question instead of selecting that camera.
- Fixed `/look` argument parsing to recognize each configured camera's `entity` value as a camera prefix.
- The HomeApp Frigate occupancy-label reducer left empty camera keys after the last active label turned off, so downstream UI could treat stale empty buckets as active label flow.
- Fixed by extracting pure camera-label helpers, escaping configured camera ids before building the occupancy regex, hydrating only currently-on labels, and deleting a camera bucket when its final active label clears.
- The People overlay's `identity_mutation` refresh path depended on discovering HA through the mutable `window.__hav_haClient` global, so opening it during HA reconnect could miss the later event subscription even though the backend event emission was covered.
- Fixed by passing the active HA client and connection state into `HomePeopleOverlay`, gating mutation subscriptions while HA is offline, keeping the legacy global fallback for compatibility, and re-evaluating the subscription when the client/connection changes.
- Simulation Mode activation intended to tear down the live HA WebSocket so real state/event streams could not leak into mock scenarios, but the effect called the nonexistent `HAClient.close()` method instead of the implemented `disconnect()`.
- Fixed by calling `HAClient.disconnect()` on Simulation Mode activation while preserving the existing credential-backed reconnect path when Simulation Mode is turned back off.
- The HA WebSocket close handler emitted `offline` for every close event, including auth-invalid closes, manual disconnects, and stale socket closes. That could overwrite the UI's "bad token" state or make Simulation Mode/device-safe teardown look like a live outage.
- Fixed by ignoring stale socket closes, keeping auth-invalid/manual disconnects quiet, and preserving offline/reconnect emission only for current live network drops.
- The bridge `/healthz` poll marked `bridgeOnline=false` on failed polls, but could leave the previous `bridgeHealth` object in state after a fetch failure, non-OK response, or bad JSON body. That could keep stale HA-connected/rooms/voice details visible during a real bridge outage.
- Fixed by clearing `bridgeHealth` on every unsuccessful bridge-health path while preserving successful JSON snapshots, and added a HomeApp source contract to `tools/run-capabilities-tests.js`.
- The vision-sidecar `/healthz` poll logged failures but did not publish an explicit reachability signal, and the runtime capability overlay treated any stale `visionHealth` object as online even when the payload reported `ok: false`.
- Fixed by tracking `visionSidecarOnline`, clearing stale vision-health data on non-OK, bad JSON, and fetch failures, treating `visionHealth.ok === false` as offline, and threading the same signal through Simulation Mode fixtures.
- The Frigate metrics hook ignored `sensor.frigate_*` transitions to `unknown` or `unavailable`, so the tray and capability chip could keep showing stale previously-good Frigate telemetry after the integration stopped reporting.
- Fixed by rebuilding the Frigate telemetry snapshot on unavailable/unknown sensor transitions and clearing stale `frigateMetrics` when the HA state reload fails.
- The bridge `/rooms` poll kept the last known room-context payload after endpoint failures, non-OK responses, or bad JSON, so future room-occupancy surfaces could show stale occupied/empty data during a bridge outage.
- Fixed by clearing `roomContext` on unsuccessful `/rooms` polls while preserving successful snapshots.
- The app polled bridge `/rooms` into `roomContext` but did not render that per-room occupancy snapshot anywhere in the metrics drawer, leaving a useful occupancy diagnostic as hidden state.
- Fixed by adding a compact occupied-first room-context section to the HA Bridge card using the existing `HmRoomRow` primitive.
- Simulation Mode did not drive `roomContext`, so the new bridge room-context UI could not be exercised in local scenario screenshots and outage scenarios would have inherited any future healthy room fixture.
- Fixed by adding a healthy bridge room-context fixture, applying `roomContext` through the Simulation Mode setter dispatcher, and clearing it in Frigate/bridge/full-outage scenarios.
- Simulation Mode could crash the entire app before first paint because a HA-derived telemetry cleanup effect read `sim.active` before the `window.useSimulation(...)` hook result was declared. The boot loader completed all files, but React rendered a blank shell with a `Cannot read properties of undefined (reading 'active')` error.
- Fixed by moving that cleanup effect below the simulation hook and adding a source-order regression in `tools/run-simulation-command-tests.js`. A local browser smoke of `?simulation=1&scenario=model-offline` now mounts without a boot overlay.
- AI Stack simulation fixtures carried `aiStackState`/`aiStackOnline`, but the Simulation Mode dispatcher ignored those keys and HomeApp did not pass the corresponding setters into `window.useSimulation(...)`. As a result the rendered model-recovery card could stay stuck at `polling` or `not configured` instead of showing recovery controls.
- Fixed by plumbing `aiStackOnline` and `aiStackState` through Simulation Mode, requiring that contract in `tools/run-simulation-command-tests.js`, and extending the `model-offline` fixture so it renders `AI STACK / DOWN`, `vllm missing`, `START AI STACK`, and service restart controls in local browser verification.
- Stage 4 production-QA smoke claimed to be a cheap autonomous UI gate, but `tools/qa-browser-smoke.js` silently required a dev server already listening on `127.0.0.1:5180`. A production-QA pass could therefore drift red because of missing setup rather than an app regression.
- Fixed by making `tools/qa-browser-smoke.js` self-host the static `app/src` shell on a loopback ephemeral port when `HOME_APP_URL` is not explicitly provided, while preserving the external URL path for callers that want to point at an already-running app.
- The hot `/metrics` poll kept the last-known model/resource snapshot after the metrics sidecar failed or returned non-OK, and `m.model || prev.model` preserved an old model name even when telemetry explicitly reported `model: null`.
- Fixed by clearing the detailed metrics snapshot/history on `/metrics` non-OK, bad JSON, or fetch failure; marking the sidecar offline on those paths; marking it online on successful samples; and treating explicit null model telemetry as model offline.
- HA-derived telemetry for camera labels, UniFi network metrics, HAOS host metrics, Frigate stats, and bridge room context stayed visible when the live HA connection left `online`, so reconnecting/offline states could still display old sensor-derived facts.
- Fixed by clearing those HA-derived telemetry buckets whenever live mode is not connected to HA, while leaving Simulation Mode fixtures authoritative.
- The UniFi/network and HAOS host metric reducers preserved old numeric values when `get_states` failed or HA sensors transitioned to non-numeric states such as `unknown` or `unavailable`.
- Fixed by clearing network/host snapshots on failed state loads, rebuilding network snapshots on bad numeric updates, pruning empty switch rows, and clearing host metric fields to `null` when their sensor updates are non-numeric.
- The metrics drawer restored only its resizable tray height across reloads, but S78 expects the drawer itself and the selected tab to survive reload. The source initialized `expanded=false` and `activeTab="ai"` every launch, so reopening on the Lab/trace tab was impossible without manual re-navigation.
- Fixed by persisting `expanded` and `activeTab` with allowlisted tab restore semantics and safe localStorage fallbacks.
- The S79 header button sweep expected the active Simulation Mode pill to open simulation controls, but the header rendered the amber sim pill as a passive `<span>` and no controls surface existed.
- Fixed by making the sim pill a real button, adding a lightweight Simulation controls dialog, and routing scenario switch/reset/exit actions through the existing `SimScenarios`/`sim` APIs.
- The S82 Vision card scenario expected hover to pause auto-refresh on the hovered camera tile, but `HomeVisionFrame` was still hard-wired with `paused={false}` for every camera.
- Fixed by tracking the hovered camera id in `HomeVisionCard` and passing that state into the existing signed-stream pause path for the matching `HomeVisionFrame`.
- The S83 People overlay scenario expected close to return focus to the header People button, but the opener had no focus ref and the overlay close path only flipped state.
- Fixed by backing the header People button with a ref, routing overlay close/Escape through a shared `closePeopleOverlay` callback, and restoring focus after the overlay unmounts.
- The S84 proactive status-row scenario expected the AI-tab row to open inline acknowledge/dismiss actions, but `ProactiveStatusLine` was passive and was not mounted in the AI tab.
- Fixed by mounting `ProactiveStatusLine` in `renderAi`, adding expandable one-shot inline actions, and routing acknowledge/dismiss through coordinator methods that clear pending timers/state and emit proactive diag events.
- The S85 AI Stack card scenario expected every stack-control verb to use the same inline `✓ confirm` flow, but the compact card only routed start/restart/stop, rendered a misleading `free gpu` label through the whole-stack stop path, and did not expose per-service restart buttons.
- Fixed by adding an exact nine-verb AI Stack action registry, separating `stop ai stack` from `free_gpu`, mapping the five per-service restart verbs to supervisor service names, and handling synchronous free-GPU/service-restart successes without requiring a task id.
- The S86 Lab diagnostics scenario expected all four global controls to be confirm-gated and the log filter to narrow visible lines, but `pause perception`/`clear cache` fired without confirmation and the filter chips owned isolated state that never reached `LabLogPane`.
- Fixed by confirm-gating all four global controls, lifting log-filter state into `LabDiagPane`, filtering the lines passed to `LabLogPane`, making `clear cache` clear the local log buffer, and wiring `pause perception` through the shared supervisor action module to stop the allowlisted `vision-sidecar` service with token and confirmation gates.
- The S87 mute-pill scenario expected the header pill reason to match the mute source, but the HA composite `binary_sensor.jarvis_muted_effective_2` exposed no `reason`/`mute_until_iso` attributes even though the Home app read them.
- Fixed by adding source-derived mute attributes for movie, TV, timer, and manual reasons, rendering the pill as a real button, clarifying that clicks clear manual/timer mute only, and pinning that header and `/unmute` paths do not clear movie/TV auto-mute signals.
- The S88 FirstRun scenario had only indirect local coverage, so endpoint/token submit, model picker behavior, picker-preserving HA reconnect events, FirstRun unmount, settings persistence, and chat-input focus release could regress independently.
- Fixed by adding a dedicated FirstRun source contract to `tools/run-bootstrap-tests.js`, tying the connect form, vLLM model discovery/picker, selected-model confirmation, prefs persistence, reconnect path, render gate, and focus-token guard together.
- The S92 Lab diagnostics log-stream scenario expected an authenticated supervisor SSE stream with abort cleanup, but the service-row `logs` action only performed a one-shot `/api/services/{service}/logs` fetch.
- Fixed by letting `HomeStackActions.logsStreamUrl` target the supervisor's current in-flight task when no `task_id` is supplied, wiring Lab service logs through `streamSse`, aborting the stream on pane close/unmount/clear-cache, escaping and bounding rendered log lines, and preserving the one-shot service-log route as a fallback.
- The S94 proactive cooldown scenario could be hidden by broad dedupe or generic recent-speech suppression before the same-room cooldown reason surfaced, and the documented step incorrectly used the `test:true` HA button that intentionally bypasses long cooldowns.
- Fixed by letting same-transport re-fires reach policy evaluation, preserving cross-transport dedupe, making same-room cooldown win over generic cooldown reasons, and updating S94 to require a non-test room-entry event for cooldown validation.
- The S95 unacked proactive notification scenario was only in memory, so a reload could clear the status-row notification even though the feed history persisted.
- Fixed by adding `hg-proactive-pending` localStorage persistence for active proactive phases, restoring it on HomeApp boot, expiring stale pending states, clearing idle states, and skipping persistence while Simulation Mode owns proactive state.
- Simulation Mode's mock light store converted `brightness_pct` to raw HA brightness with `pct * 2.55`, which rounded 50% down to 127 due to floating-point precision instead of the intended midpoint 128.
- Fixed the conversion to use `(pct / 100) * 255`, matching HA-style raw brightness mapping more closely.
- Simulation Mode outage scenarios shallow-merged `cameraLabels` with the healthy baseline, so `frigate-offline` and `everything-offline` could still look like active person-detection flows.
- Fixed `cameraLabels` to be scenario-authoritative replacement data and cleared the outage fixtures so stale healthy labels cannot survive an offline Frigate simulation.
- The spatial simulation fixture exposed a real private-LAN Frigate URL.
- Fixed the fixture to use `http://sim-frigate.local:5000`, preserving shape without leaking a live-network endpoint.
- `ha-config/extended_openai_conversation/test_living_lights.py` could fail when run directly from a Windows cp1252 console because its Unicode section headers were written without forcing UTF-8.
- Fixed the standalone test harness to reconfigure stdout/stderr to UTF-8, matching the coverage orchestrator's environment and keeping local dev runs clean.
- The S2S voice bridge URL helper downgraded configured `wss://` bases to `ws://`, and accepted unsupported URL protocols by silently coercing them into WebSocket URLs.
- Fixed `bridgeWsUrl` to preserve secure `wss://` bases, map only `http:`/`ws:` to `ws:`, map only `https:`/`wss:` to `wss:`, and reject unsupported protocols.
- The metrics tray `PipelineBar` trusted partial telemetry durations too much, so non-finite stage values, negative durations, or stage sums larger than the reported total could render invalid or oversized percentage widths in the diagnostics drawer.
- Fixed by normalizing pipeline stage telemetry, clamping negative durations to zero, dropping non-finite stages, falling back to the summed stage duration when total time is absent, and scaling the visual bars against the larger of reported total and summed stages.
- The compact AI Stack card treated `overall: "down"` differently from the Lab recovery policy: Lab classified it as offline, but the card did not mark it critical or show the `start ai stack` recovery action.
- Fixed by making `"down"` a critical/startable compact-card state and extracting the action-visibility policy into a pure helper.
- The read-only kitchen lighting watcher captured the right raw events, but its summary did not identify the exact failure signature where a kitchen light command requested high brightness and the lights later ended low/off.
- Fixed by adding diagnosis hints for high-brightness service calls that fail to stick, later low kitchen `light.turn_on` calls after a high user command, sampled brightness drops, asleep-mode precedence during a failed hold, and broad-occupancy-vs-zone sensor gaps.
- The same watcher did not subscribe to `living_lights_manual_hold_armed` / `living_lights_override_detected`, so a live capture could prove the kitchen lights dimmed again but not whether the fast manual override hold actually armed.
- Fixed by subscribing to those events, preserving their key debug fields in JSONL/Markdown, adding a manual-hold summary section, and flagging the exact case where a high command fails to stick with no `living_lights_manual_hold_armed` event.
- The read-only Frigate/HA occupancy watcher captured Frigate camera stats but did not render them in the markdown diagnosis, so a watch where occupancy failed could under-explain whether Frigate was misclassifying movement or the camera/detection pipeline itself was unhealthy.
- Fixed by adding camera-health summarization for disabled detection and zero camera/process/detection FPS, and by prioritizing those failures before HA mirror/classifier/cascade diagnoses.
- The same watcher treated all Frigate-person-without-HA-occupancy captures as one mirror/integration branch, even though Living Lights specifically needs the person event to land in a watched lighting zone before fade-on can be expected.
- Fixed by reading zones from Frigate's top-level and `data.*` event payload shapes, normalizing zone names, and splitting the diagnosis between "person outside watched zones" and "person in watched zone but HA did not mirror it."
- Existing Frigate/HA watcher JSONL captures could not be re-rendered after diagnosis logic improved, so the evidence markdown named in the RC blocker could lag behind the raw capture without re-polling live Frigate/HA.
- Fixed by adding an offline `--from-jsonl` re-render path and refreshing `tools/reports/frigate-occupancy-watch-living_room-2026-06-23-084610.md` from the already captured JSONL.
- The kitchen watcher evidence from `tools/reports/kitchen-lighting-watch-2026-06-23-073656.jsonl` showed Living Lights repeatedly re-sending asleep-capped `30%` actuator calls for classifier updates where the zone state and predicted brightness had not changed (`present 30% -> present 30%`).
- Fixed by adding a generated actuator guard that skips same-state classifier churn while still allowing real state transitions, direct Frigate wakeups, asleep flips, shadow cutover, time-pattern reevaluation, and dedicated `predicted_brightness_pct` changes.
- The manual cooldown gate was not pinned to the classifier's possible `asleep` state in tests, so a future/as-deployed asleep-state branch could bypass manual holds despite the intended "manual touches while asleep still stick" policy.
- Fixed the generator and renderer coverage so `asleep` is treated as a managed cooldown state alongside `vacant`, `present`, and `pass_through`.
- The aggregate manual-detection package had drifted from `build-living-lights-actuators.py` because voice-reset automations were emitted by iterating a Python `set`, making their order nondeterministic between generator runs.
- Fixed by sorting the voice-reset zone order in the generator and regenerating `ha-config/packages/living_lights_manual_detection.yaml` from the deterministic source.
- The shared AI stack action client concatenated supervisor URLs directly, so a saved/configured trailing slash could produce double-slash API paths for start/restart/stop/status/log-stream requests.
- Fixed by normalizing supervisor base URLs inside `HomeStackActions` before constructing whole-stack, per-service, status, and log-stream routes.
- The QA registry frontend inventory only counted literal `<script src=...>` tags, so after the custom boot loader landed it reported 3 frontend scripts and missed the app's actual ordered module graph.
- Fixed `tools/qa-registry-audit.py` to merge static scripts with the `index.html` boot-loader `files = [...]` array, raising the documented frontend script inventory to 48.
- The AI Stack card delegated start/restart/stop POSTs to the normalized shared action client, but still concatenated the SSE log-stream URL directly. A saved supervisor URL with a trailing slash could produce a double-slash log URL after dispatching model recovery actions.
- Fixed the AI Stack card to build log-stream URLs through `HomeStackActions.logsStreamUrl` when available, with a local trailing-slash-normalized fallback.
- The AI Stack recovery card treated `overall: "down"` as critical/startable, but the AI tab dependency strip used a separate inline tone map and still rendered `down` as idle.
- Fixed the dependency strip to derive its AI Stack chip through a shared helper that marks `down`, `offline`, and `error` as critical while preserving the existing warning and unknown/no-token states.
- The AI Stack health poller cleared `aiStackState` on rejected `STACK_TOKEN`, rate limiting, bad JSON, or status-fetch failures without preserving the reason, so the card could collapse a reachable supervisor plus bad status/auth into a vague offline/no-token recovery path.
- Fixed by tracking `aiStackStatusError`, clearing stale stack state on failed status probes, and rendering distinct `auth failed`, `rate limited`, and `status unavailable` states in both the AI Stack card and compact dependency strip.
- A failed `START AI STACK` run could show only `exit 1` plus raw Docker logs, even when the streamed task output clearly showed Docker reached the NVIDIA/OCI runtime prestart hook and failed before the AI process launched.
- Fixed by adding AI Stack terminal failure diagnosis for GPU runtime/NVIDIA toolkit failures, disk-full errors, port conflicts, unavailable images, Docker daemon failures, and unknown stack-action failures; the exact OCI/prestart/`legacy` failure shape is now covered in `tools/run-ai-stack-card-tests.js`.
- The Jarvis mute contract covered voice/proactive silence while muted, but did not assert that explicit typed Home-app commands remain usable. That let TV auto-mute silently swallow typed commands such as `turn off all my lights`, leaving only the user bubble and no visible error.
- Fixed by routing typed Home-app sends through the existing one-turn transient-unmute phrase while the header mute state is active, preserving voice mute behavior but allowing explicit typed commands to execute; the mute pill fallback now says `active` instead of falsely defaulting to `manual`.
- `docs/HOME_SYSTEM_OVERVIEW.md`, `docs/RUNBOOK.md`, and the stack-supervisor module docstring still described the supervisor as read-only/Phase 1 even though start/restart/stop/log/free-GPU routes are implemented and covered.
- Fixed by updating the supervisor API documentation to the current route surface and adding a static drift check that fails if those docs regress to the old Phase 1 wording while mutating routes exist.
- `/agent-tools` described itself as the full LLM tool inventory but had drifted from `DEFAULT_CONF_FUNCTION_TOOLS`, omitting the Living Lights presence-override tools (`set_presence_override`, `clear_presence_override`, `get_recent_overrides`) and gesture-training capture tool.
- Fixed by adding the missing tools to the visible grouped inventory and by comparing the frontend `/agent-tools` list directly with the backend registry in the slash-command suite.
- `ha-config/extended_openai_conversation/test_native.py` could fail when run directly from a Windows cp1252 console because its Unicode section headers were written before the actual assertions executed.
- Fixed the standalone native test harness to reconfigure stdout to UTF-8 so the plain `py -3 ha-config\extended_openai_conversation\test_native.py` command used by feature probes and local dev runs reaches the real backend tool assertions.
- `ha-config/extended_openai_conversation/test_lifecycle.py` could also fail when run directly from a Windows cp1252 console because existing test names print Unicode arrows before the suite reaches the HA lifecycle/CORS assertions.
- Fixed the standalone lifecycle harness to reconfigure stdout/stderr to UTF-8, matching the coverage orchestrator's environment.
- `docs/HOME_SYSTEM_OVERVIEW.md` had drifted behind the current Home frontend: section 8 only documented a partial older feature table, while the boot loader now loads 45 first-party modules including `/lights`, `/apartment`, Home Intelligence, the video labeler, Simulation Mode helpers, and Living Lights event subscribers.
- Fixed by adding a boot-loader module inventory to section 8 and by extending the Living Lights automation pattern docs with the current direct Frigate wake triggers, asleep trigger, same-state classifier churn guard, and `asleep` manual-cooldown state.
- The stack supervisor's status/action maps lagged behind the default compose stack: `chatterbox-tts` and `intelligence` were started by `docker compose up -d` but were absent from supervisor status and per-service logs/restart/stop controls.
- Fixed by adding Chatterbox and Intelligence probes/actions/aliases to `stack/services/supervisor/main.py`, then extending the supervisor regression suite so status probes and service actions must cover every default compose service.
- The Lab diagnostics pane still hard-filtered the supervisor service list to an older four-service subset (`vllm`, `wyoming-parakeet`, `kokoro`, and `vision-sidecar`), hiding Chatterbox TTS, `wyoming-kokoro`, `metrics-sidecar`, and Home Intelligence even after the supervisor exposed them.
- Fixed by passing the full supervisor-derived `services` list into `LabDiagPane`, so model/service recovery controls stay visible for every default compose service.
- The underlying `buildStackList()` source still emitted only the old four managed services plus infra rows, so the diagnostics pane could receive a "full" prop that was already missing Chatterbox TTS, `wyoming-kokoro`, `metrics-sidecar`, and Home Intelligence.
- Fixed by deriving the list from a canonical `STACK_SERVICE_ORDER` that matches the default Docker Compose service inventory (with the `kokoro-tts` compose service displayed as `kokoro`), adding missing service metadata, and showing non-pipeline managed services in the flow-view legend.
- The feature-validation probe matrix still left the GPU temp pill and multi-frame vision behavior with static/live-only evidence, so the local gate could not prove those feature probes had executable local coverage.
- Fixed by adding local executable Tier 2 probe bindings for GPU temp (`node tools/run-lab-tests.js`) and multi-frame vision (`node tools/run-vision-tests.js`), and by adding a meta-test that every feature probe must include a local executable tier.
- The live F-3 multi-frame vision probe still posted the stale `frame_count` field even though the sidecar contract now accepts `frames`, so a live probe would have tested the wrong request shape.
- Fixed by switching the probe body to `frames` and pinning the allowlisted F-3 POST contract to `/describe_clip`, private/local URLs, bounded frame counts, and no stale `frame_count` field.
- The generated QA audit documented many source-derived surfaces but did not expose the exact Living Lights classifier states or HA `input_boolean` flags, making asleep/manual-override precedence harder to audit from release evidence.
- Fixed by extracting the classifier state machine and every repo-defined HA `input_boolean` flag into `docs/qa/home-app-feature-audit.md/json`, and by adding a Home overview drift guard for the classifier state order plus the current flag count/link.
- `docs/HOME_SYSTEM_OVERVIEW.md` still claimed the Extended OpenAI Conversation tool catalog had no aggregated list, even though the backend registry and `/agent-tools` surface now need to stay aligned for model-command debugging.
- Fixed by extracting the 23-tool backend registry from `DEFAULT_CONF_FUNCTION_TOOLS` into `docs/qa/home-app-feature-audit.md/json`, linking that generated catalog from the overview, and adding a static overview drift guard for the current count/link/stale-claim wording.
- `docs/HOME_SYSTEM_OVERVIEW.md` still listed the `spatial_model.json` schema as undocumented, described local `camera_edges`/`zone_to_room` as present-but-empty, and claimed workshop lacked light polygons even though the current repo model omits those deployed-only fields and contains drawn workshop footprints.
- Fixed by extracting the local schema 2 spatial model into `docs/qa/home-app-feature-audit.md/json`, documenting the 4-camera/16-light current shape, and distinguishing the local repo copy from the deployed HAOS copy that still carries canonical `camera_edges`/`zone_to_room`.
- `docs/HOME_SYSTEM_OVERVIEW.md` still treated the `predictive-lighting` add-on MQTT schema as an inferred source-code detail instead of a first-class integration contract, even though those topics are the bridge from Frigate trajectory prediction into HA `anticipated_*` binary sensors.
- Fixed by extracting the add-on's MQTT publish/subscribe topic families from `addons/predictive-lighting/app.py`, documenting the 5 publish families and 3 subscription families, and linking the generated schema from the overview.
- `docs/HOME_SYSTEM_OVERVIEW.md` still grouped newly source-derived inventories under a "Documentation gaps" heading, making release evidence look missing even after the generated audit and static guards covered those surfaces.
- Fixed by splitting the section into source-derived inventories covered by generated QA evidence versus true remaining documentation gaps.
- `docs/HOME_SYSTEM_OVERVIEW.md` still listed the stack-supervisor endpoint reference as a future documentation gap, and `docs/RUNBOOK.md` overstated `X-Confirm` requirements for start/restart routes that are bearer-token gated but not confirmation-gated in source.
- Fixed by adding a dedicated stack-supervisor endpoint reference to the runbook, correcting the confirmation-header scope, and linking the source-derived endpoint inventory from the overview.
- `docs/HOME_SYSTEM_OVERVIEW.md` still listed a missing ADR trail for major pivots as a remaining documentation gap even though the evidence lived in scattered experiment notes, compose comments, and source-level rationale.
- Fixed by adding `docs/ARCHITECTURE_DECISIONS.md`, consolidating the Markov-to-kinematic, PersonaPlex/Moshi-to-split-stack, Chatterbox/Kokoro, and Qwen3-VL model-size decisions with rollback paths and evidence links.
- The desktop release ledger still treated Tauri packaging as unverified because the default `target/release/home.exe` had been locked, even though the no-bundle binary build could already use an isolated target directory.
- Fixed by running the full Tauri bundle build in `target-rc-bundle`, verifying the generated Windows MSI with a non-install administrative extraction, and narrowing the ledger item from a full blocker to a residual clean-machine install-smoke risk.

Coverage added in this pass:

- Added `tools/watch-frigate-occupancy.py`, a read-only diagnostic watcher that compares direct Frigate event labels with Home Assistant occupancy/classifier entities.
- Added `tools/test-watch-frigate-occupancy.py`, a pure local regression suite for the watcher's report classification branches: Frigate camera-health failures, Frigate person event-zone parsing, person outside watched lighting zones, Frigate person event with no HA occupancy, non-person Frigate labels only, HA occupancy with vacant classifier, classifier non-vacant/downstream cascade focus, and offline JSONL re-rendering.
- Added `tools/run-lighting-events-tests.js`, a pure local suite for the app's Living Lights HA event subscriber: manual override rendering, debug context, articulation entries, timestamp fallback, subscription teardown, and subscription-error isolation.
- Registered `tools/test_living_lights_cooldown_gate.py` in the quick gate and gave it a standard footer, covering the core contract that manual holds stick while asleep, classifier `asleep` remains cooldown-managed, and external Lutron/HomeKit/Siri-style changes arm the cooldown before the pilot can re-dim.
- Extended `tools/test_living_lights_cooldown_gate.py` so every generated fast manual-hold armer is immediate, prediction-aware, independent of HA `parent_id` context filtering, appears before the slower settled detector, stamps every owning zone's manual timestamp/context id, emits `living_lights_manual_hold_armed`, and keeps the checked-in manual-detection package fresh against the generator.
- Extended the same suite to require aggregate room/group fast armers for the known managed entities, while keeping settled preference detection limited to physical leaf lights.
- Added `tools/run-stack-actions-tests.js`, a pure local suite for the `HomeStackActions` client: supervisor URLs, bearer headers, destructive-action confirm headers, task-id extraction, and HTTP/network error mapping.
- Added `tools/test-supervisor-service-actions.py`, a pure local suite for the stack supervisor's new allowlisted service routes. The test stubs FastAPI/httpx when absent and monkeypatches compose execution, so it does not touch Docker or the live AI stack.
- Extended `tools/test-supervisor-service-actions.py` so the supervisor's status probe list and service-action allowlist must cover every default service in `stack/docker-compose.yml`, including Chatterbox TTS and Home Intelligence.
- Added `tools/run-sse-fetch-tests.js`, a pure local suite for the SSE-via-fetch helper used by stack log streams: auth headers, chunked frames, JSON and raw data, done events, HTTP/reader errors, abort handling, and closeout.
- Extended `tools/run-lab-tests.js` with stack-recovery tiering coverage for the Lab/AI model controls: offline stack exposes start, crashed/degraded services expose logs/restart, in-flight start shows warming progress, and destructive model actions stay confirm-gated.
- Extended `tools/run-lab-tests.js` so the Lab diagnostics pane must receive the unfiltered supervisor service list, keep service log/stop/restart verbs wired, and parse the current default compose services including Chatterbox TTS, `metrics-sidecar`, and Home Intelligence.
- Extended `tools/run-lab-tests.js` so `STACK_SERVICE_ORDER` must match the default Docker Compose service order, every emitted stack-list row must have `SERVICE_META`, and non-pipeline managed services must remain visible in `FLOW_LEGEND_SERVICES`.
- Added `tools/test_living_lights_frigate_occupancy_path.py`, a pure local generator/package invariant suite for the Frigate zone occupancy path: raw zone sensors, stable de-flapped sensors, kitchen camera fallback, classifier triggers/state reads, asleep cap behavior, checked-in package freshness, direct actuator wakeups for every actuated zone, present/fade-on fast-ramp and slow-ramp action flow, same-state classifier churn suppression, and shared-light vacant co-controller guards.
- Added `tools/run-ha-client-tests.js`, a pure local suite for HA websocket helper behavior: websocket URL mapping, `call_service` payload shaping, `callService(...)` convenience method behavior, target arrays, disconnected rejection, network-drop reconnect, auth-invalid close handling, and quiet manual disconnect.
- Added `tools/run-lights-drawer-tests.js`, a pure local suite for the `/lights` drawer service-call wrapper and sleep/night-safe wiring: `callService` preference, raw fallback payload shaping, offline guard messaging, `homeai_sleep` subscription, disabled sleep controls while HA is offline, disabled HA-backed sliders/scope/footer actions, and cooldown-sweeper offline gating.
- Added `tools/run-control-card-tests.js`, a pure local suite for inline light/media control-card helpers: target construction, lifecycle derivation, simulation-vs-real dispatch, light capabilities, and media capability/title parsing.
- Added `tools/run-slash-command-tests.js`, a pure local suite for slash-command catalog/handler coherence: visible command handlers, deliberate hidden aliases, autocomplete spacing, `/route-log` exposure, retired `/s2s on|off` behavior, and the `/lab-dump-watch` argument regression.
- Extended `tools/run-slash-command-tests.js` with source-level command-surface contracts for `/help`, `/world-state`, `/cameras`, `/describe-clip`, and `/agent-tools`: click-to-fill wiring, per-room world-state drawer filtering, Simulation Mode camera/clip fixtures, no-live-call fallback guards, tolerant clip parsing, and backend-vs-frontend tool inventory drift detection.
- Updated `tools/feature-validation-probes.json` so `/describe-clip`, `/agent-tools`, `/world-state` per-room filtering, `/cameras`, and categorized `/help` now point at the executable slash-command suite instead of remaining Tier-1-only grep checks.
- Extended `tools/run-simulation-command-tests.js` with Simulation Mode hook-order and AI Stack fixture-dispatch contracts, and extended `tools/run-simulation-scenario-tests.js` so `model-offline` must expose a local AI Stack recovery state.
- Extended `tools/run-capabilities-tests.js` with an F-18 HomeApp source contract for the capability chip: Simulation Mode suppression, `buildStackList`/`summarizeCapabilityHealth` derivation, healthy-state hiding, critical offline color, tooltip text, and chip text rendering.
- Extended `tools/run-capabilities-tests.js` with runtime outage overlay coverage so HA, bridge, TTS, vision-sidecar, metrics-sidecar, Frigate-stale, and Frigate-unknown signals map to the same service/capability vocabulary used by the header and AI drawer.
- Extended `tools/run-capabilities-tests.js` with HomeApp source contracts for metrics-sidecar stale snapshot clearing, explicit null model telemetry, and placeholder/default model suppression in the dependency strip.
- Extended `tools/run-capabilities-tests.js` with HomeApp source contracts for HA-disconnect telemetry clearing plus UniFi/network and HAOS host stale-metric cleanup.
- Extended `tools/run-lab-tests.js` with S78 tray-state persistence coverage: expanded/collapsed restore, valid tab restore, invalid legacy tab fallback, and source contracts for the new drawer open/tab storage keys.
- Extended `tools/run-simulation-command-tests.js` with S79 header-control coverage: the sim pill must be clickable, HomeApp must own and mount the Simulation controls dialog, and the dialog must reuse existing scenario APIs.
- Extended `tools/run-slash-command-tests.js` with S80 InputRow coverage: focus-token guarding, slash filtering and keyboard/mouse completion, send/run rendering, stop-button cancellation wiring, mic-toggle wiring, and the `stopStreaming` cleanup path.
- Extended `tools/run-control-card-tests.js` with S81 light-card sweep coverage: the brightness/color-temperature slider commit semantics, warm/neutral/cool/dim/bright quick chips, shared `light.turn_on` dispatch path, and error dismiss behavior.
- Updated `tools/feature-validation-probes.json` so the remaining Tier-1-only rows now point at executable checks: M5+ tool timeline (`run-explain-tests`), F-18 and capability enrichment/fallback hints (`run-capabilities-tests`), F-7/F-9 backend tool behavior (`test_native.py`), and F-32+ world-state auto-refresh countdown (`run-worldstate-tests`). A matrix scan now reports `tier1_only=0`.
- Extended `tools/test-qa-feature-recipes.py` so the feature probe matrix must stay locally backed for every feature, and live HTTP probes must stay scoped to local/private endpoints with F-3 as the only POST allowlist.
- Extended `tools/qa-static-checks.py` with a source-derived documentation drift guard: every file in `app/src/index.html`'s boot-loader `files = [...]` array must be mentioned in section 8 of `docs/HOME_SYSTEM_OVERVIEW.md`. The check currently proves 45 frontend modules are documented.
- Extended the same overview drift guard to require every static frontend runtime script from `app/src/index.html` to be documented as well, covering React, ReactDOM, and Babel standalone in addition to the 45 app modules.
- Added the current 51-command slash-command inventory to `docs/HOME_SYSTEM_OVERVIEW.md` and extended static QA so section 8 must mention every alias extracted from `home-app.jsx`'s command handler.
- Added the current 62-scenario Simulation Mode inventory to `docs/HOME_SYSTEM_OVERVIEW.md` and extended static QA so section 8 must mention every scenario ID extracted from `simulation-data.jsx`.
- Tightened those overview inventory guards so slash-command and Simulation Mode scenario sections must be exact: stale documented commands/scenarios now fail static QA just like missing ones.
- Tightened the overview frontend inventory guards the same way: stale boot-loader module rows or stale runtime-script rows now fail static QA, not only missing current entries.
- Added a source-derived major UI surface inventory to `docs/HOME_SYSTEM_OVERVIEW.md` and extended static QA so the documented list of drawer/overlay/modal/panel/lightbox surfaces must stay exact.
- Added source-derived Living Lights state/flag inventory coverage: `tools/qa-registry-audit.py` now lists the 8 classifier states and all 50 repo-defined HA `input_boolean` flags, `tools/test-qa-readiness-log.py` requires the generated markdown to expose those lists, and `tools/qa-static-checks.py` keeps the Home overview's classifier state order plus flag-count/link synchronized.
- Added source-derived Extended OpenAI agent-tool inventory coverage: `tools/qa-registry-audit.py` now lists all 23 backend tools from `DEFAULT_CONF_FUNCTION_TOOLS`, `tools/test-qa-readiness-log.py` requires the generated markdown to expose those rows, and `tools/qa-static-checks.py` keeps the overview's tool count/link synchronized while rejecting the stale "no aggregated list" claim.
- Added source-derived spatial-model schema coverage: `tools/qa-registry-audit.py` now lists the local `spatial_model.json` schema, camera/zone/light counts, missing deployed-only fields, and per-camera zone rows; `tools/test-qa-readiness-log.py` requires those markdown rows; and `tools/qa-static-checks.py` keeps the overview's schema/count/link/local-vs-deployed wording synchronized.
- Added source-derived predictive-lighting MQTT schema coverage: `tools/qa-registry-audit.py` now lists publish/subscribe topic families from `addons/predictive-lighting/app.py`, `tools/test-qa-readiness-log.py` requires those generated markdown rows, and `tools/qa-static-checks.py` keeps the overview's MQTT topic counts/link/topic strings synchronized.
- Added source-derived stack-supervisor endpoint coverage: `tools/qa-registry-audit.py` now lists all 12 FastAPI decorators from `stack/services/supervisor/main.py` with status/auth/confirm semantics, `tools/test-qa-readiness-log.py` requires those generated markdown rows, and `tools/qa-static-checks.py` keeps the runbook endpoint table plus overview count/link synchronized.
- Added an ADR-trail guard in `tools/qa-static-checks.py` so `docs/ARCHITECTURE_DECISIONS.md` must retain the four release-facing architecture decisions, rollback notes, evidence links, and overview inventory linkage.
- Extended `tools/test-rc-blocker-ledger.py` so the desktop bundle/install ledger item must stay aligned with the current MSI build and administrative-extraction evidence, and must not regress to the stale locked-executable blocker wording.
- Added a documentation-inventory status guard in `tools/qa-static-checks.py` so resolved source-derived inventories stay out of the remaining-gap list.
- Added a source-derived Home Assistant package inventory to `docs/HOME_SYSTEM_OVERVIEW.md`, corrected the stale 19-package count to 24, and extended static QA plus the generated audit so package drift is caught.
- Added a source-derived Docker Compose service inventory to `docs/HOME_SYSTEM_OVERVIEW.md`, refreshed `docs/RUNBOOK.md`'s "what runs where" table, and extended static QA plus the generated audit so default/profile service drift is caught.
- Extended `tools/qa-registry-audit.py` so the generated markdown audit lists every discovered Home Assistant view and first-party FastAPI route, and extended readiness-log coverage so those markdown inventories must match the generated JSON.
- Extended the generated markdown audit to list every discovered frontend script from `app/src/index.html`, covering both CDN runtime scripts and the boot-loader module graph. Readiness-log coverage now checks the markdown script inventory against the generated JSON.
- Extended readiness-log coverage so generated audit markdown static inventory lists for slash commands, Simulation Mode scenarios, and UI surfaces must match the generated JSON.
- Extended the generated markdown audit to list every non-doc canonical QA entry group: live agent scenarios, screenshot recipes, feature probes, and quick/unit suites. Readiness-log coverage now checks those markdown rows against the generated JSON registry.
- Extended the generated markdown audit to list every documented `DOC-S*` scenario with its evidence status, local evidence files, and remaining live/manual work. Readiness-log coverage now checks those scenario rows against the generated JSON evidence map.
- Added `tools/scenario-evidence-map.json`, a source-controlled evidence map for all 99 documented `DOC-S*` scenarios. It separates `simulation_green`, `local_contract_green`, `local_partial_green`, and `live_required` so local mock coverage is not overstated as a live pass.
- Added and registered `tools/test-scenario-evidence-map.py`, validating that every documented scenario is mapped, every non-live-required scenario cites local evidence, every evidence file exists, and every scenario records remaining live/manual work.
- Extended `tools/qa-registry-audit.py` to consume the scenario evidence map, annotate documented scenarios with honest local/live result states, and include the scenario evidence summary in `docs/qa/home-app-feature-audit.md/json`.
- Extended `ha-config/extended_openai_conversation/test_lifecycle.py` with an S23 CORS source contract: the CORS base view opts into HA middleware, every custom REST view inherits it, identity PATCH/DELETE handlers are CORS-enabled, and executable view code does not set duplicate `Access-Control-*` headers.
- Added and registered `tools/test-jarvis-mute-contract.py`, covering S21's local mute lifecycle wiring across `homeai_jarvis_mute.yaml`, `conversation.py`, the Home app `/mute`/`/unmute`/header pill paths, and `tools/probe-voice-stories.py` mute-aware skips.
- Added and registered `tools/test-workflow-scenarios.py`, covering the workflow scenario model used by `diagnose-identity.py --workflow`: scenario registry shape, safe-target guard, malformed-argument and hallucinated-domain invariants, scored Phase 3 mute semantics (`expect_silent`, REST speech, and transient-unmute SSE aliases), relax/cozy vs focus lighting predicates, and live-runner attachment of the safety guard/invariants.
- Extended `tools/workflow_scenarios.py` with a local-only `PlanningContract` catalog for DOC-S58 through DOC-S68. These contracts pin each story's intended checks, actions, fallbacks, forbidden outcomes, and live-validation caveat without adding them to the default live W runner, so they cannot accidentally actuate full-apartment scenes.
- Added and registered `tools/test-qa-readiness-log.py`, which keeps the readiness log synchronized with the generated QA audit and local suite registry so entry counts, scenario evidence counts, gate counts, and live-validation caveats cannot silently drift.
- Added and registered `tools/test-qa-feature-recipes.py`, which pins the deferred live agent and screenshot QA recipes to stable IDs, write-gated safe light targets, strict/clear/redact privacy modes, and generated-audit entries without executing live-home effects.
- Added `docs/qa/home-app-rc-blockers.json` and registered `tools/test-rc-blocker-ledger.py`, which keeps full-RC blockers and residual risks tied to evidence paths, readiness anchors, and explicit unblock conditions.
- Extended `tools/test-rc-blocker-ledger.py` so every structured blocker/residual-risk ID and title must be visible in this readiness log, not only stored in JSON.
- Added Rust unit coverage in `app/src-tauri/src/main.rs` and registered it as `cargo-test-tauri` in `tools/run-test-coverage.py`, pinning the thin Tauri shell's single-instance focus behavior, runtime plugin dependencies, main-window manifest, bundle targets, asset protocol scope, and default capability permissions.
- Added `tools/run-proactive-tests.js`, a pure local suite for the proactive coordinator policy and flow: event normalization, suppression rules, confirmed arrival welcome, return-home scene dispatch, follow-up transitions, room prompts, multi-room suppression, left-home state, weak identity handling, Simulation Mode inertness, and S84 status-row acknowledge/dismiss behavior.
- Added `tools/run-external-tests.js`, a pure local suite for external-reasoning frontend helpers: classifier routing, route explanations, Simulation Mode fetch guard, privacy payload shape, stats recording, no-key/provider failure mapping, bare-prompt provider dispatch, S96-S99 `/ask`/status/GENERAL/failure contracts, and the in-app external suite runner.
- Added `tools/run-look-tests.js`, a pure local suite for `/look` visual-reasoning helpers: fallback/custom camera roster parsing, longest-prefix matching, `camera.<id>` entity-id prefixes, sidecar URL derivation, and `/look` slash-command drawer wiring.
- Added `tools/run-video-labeler-data-tests.js`, a pure local suite for the video-labeler data layer: Simulation Mode URL/API guards, no-throw service wrappers, request payload shaping, ontology axis mapping, draft validation hygiene, media URL helpers, label metadata helpers, and timeline segment math.
- Added `tools/run-tauri-glue-tests.js`, a pure local suite for the Tauri runtime glue: Simulation Mode fetch blocking, Tauri HTTP vs browser fetch selection, localStorage-backed prefs/events/conversation/onboarding state, event trimming and transient flag stripping, and no-op/real window controls.
- Added `tools/run-intelligence-tests.js`, a pure local suite for Home Intelligence helpers: sidecar base derivation, no-store fetch wrapper behavior, lighting value/transition formatting, preference/override capture summaries, and empty-frame explanations.
- Added `tools/run-vision-tests.js`, a pure local suite for the Vision card helper/source contracts: stable camera roster, HA signed-stream base derivation, stream re-sign/remount recovery, Simulation Mode live-stream bypass, label-driven occupied-camera UI wiring, click-to-center selection, dwell-based carousel selection, and hover refresh pause.
- Extended `tools/run-vision-tests.js` with a source contract for the vision sidecar `/describe_clip` endpoint: the request uses `frames`, clamps frame count, samples multiple camera-proxy frames, reports sampling/inference latency, and returns `frames_captured`.
- Added `tools/run-apartment-data-tests.js`, a pure local suite for the `/apartment` data layer: model load/save fallbacks, remote-cache/draft/seed priority, HA registry palette filtering, bound-state subscriptions, service-call payloads, Simulation Mode tracks, and spatial-tracker WebSocket wiring.
- Added `tools/run-app-frigate-label-tests.js`, a pure local suite for HomeApp Frigate occupancy-label reducers: initial `get_states` hydration, `state_changed` adds/removes, aggregate `all` skips, unconfigured-camera ignores, label normalization, and hook wiring.
- Added `tools/run-simulation-control-tests.js`, a pure local suite for the Simulation Mode control store: mock light/media HA-shaped state, subscriber notifications, light on/off/toggle behavior, brightness/color-temperature writes, media transport/volume/mute, media grouping, and reset fidelity.
- Added `tools/run-simulation-command-tests.js`, a pure local suite for Simulation Mode command/session behavior: browser URL boot activation, Tauri boot guard, legacy localStorage cleanup, `/simulation` and `/sim` command routing, scenario help/list output, session-only persistence, and timeline cancellation contracts.
- Extended `tools/run-simulation-command-tests.js` so Simulation Mode containment pins the app-level HA WebSocket teardown on activation and credential-backed HA reconnect on deactivation.
- Added `tools/run-simulation-camera-tests.js`, a pure local suite for Simulation Mode camera fixtures: SVG placeholder state rendering, JPEG-vs-SVG selection, missing-state fallback behavior, and privacy checks for LAN URLs and HA entity ids.
- Added `tools/run-simulation-scenario-tests.js`, a pure local suite for Simulation Mode scenario fixtures: scenario registry coherence, baseline merge behavior, offline Frigate/everything-offline label invariants, and sim-only explain/world-state/spatial/camera/clip fixture contracts.
- Extended `tools/run-lights-drawer-tests.js` with coverage for the real `computeCTSliderEffective` helper extracted from `home-lights.jsx`, including ToD inheritance, explicit CT override precedence, string coercion, invalid ToD fallback, and negative/NaN override handling.
- Extended `tools/run-people-tests.js` with a DOC-S13 source contract for the People overlay `identity_mutation` refresh path: explicit HA client/connection props, reconnect gating, legacy global fallback, event subscription, 500 ms debounce, cleanup, and client/connection dependency tracking.
- Extended `tools/run-people-tests.js` with S83 People overlay interaction coverage: refresh button wiring, close/Escape behavior, graph/list/queue tab switching without selection loss, graph-node and list-row detail selection, detail-panel close, and header focus restoration.
- Extended `ha-config/extended_openai_conversation/test_living_lights.py` with `get_recent_overrides` coverage for missing world-state aggregator fallback, default collapsed sessions, zone/hour normalization, raw override-event mode, exception fallback, dispatcher routing, and preservation of automation-tail learning fields.
- Registered `ha-config/extended_openai_conversation/test_override_sessions.py` in `tools/run-test-coverage.py`, covering manual override burst collapse, intentional-off sessions, automation-tail detection, confidence bands, and the canonical downstream fixture contract.
- Added `tools/run-s2s-tests.js`, a pure local suite for the S2S voice bridge helpers: bridge URL mapping, token encoding, unsupported-protocol rejection, Simulation Mode no-live-call guard, invalid URL/mic failure guards, hello/voice-mode WebSocket protocol, mic PCM conversion, bridge callbacks, runtime voice/voice-mode commands, error closeout, and analyser cleanup.
- Added `tools/run-events-tests.js`, a pure local suite for the chat feed event contracts: exported renderer/helper surface, `/help` click-to-fill wiring, explainability click marker, controllable light/media card preference, speaker mapping, consecutive turn grouping, group timestamp behavior, event identity preservation, and the action undo inverse map.
- Added `tools/run-metrics-tests.js`, a pure local suite for the metrics tray contracts: exported `Hm*` component surface, `MetricCard` variance gating, dependency/room/Sparkline static wiring, `_hasVariance` edge cases, and `PipelineBar` telemetry normalization.
- Added `tools/run-ai-stack-card-tests.js`, a pure local suite for the compact AI Stack card and AI dependency strip: status tone mapping, service state chips, token/offline/auth/rate-limit/status-unavailable copy, dependency-strip `down`/offline/error/auth critical states, `HomeStackActions` delegation, normalized log-stream URL construction, start/restart/stop/free-GPU/service-restart affordance visibility, S85's exact nine-verb confirm matrix, busy/progress suppression, terminal result states, poller status-error wiring, and the `overall: "down"` recovery regression.
- Extended `tools/run-lab-tests.js` with S86 Lab diagnostics interaction coverage: diag toggle wiring, all four global buttons confirm-gated, per-service logs/stop/restart wiring, log-filter state feeding the rendered log pane, local clear-cache behavior, and `pause perception` dispatch through the shared `vision-sidecar` supervisor stop action.
- Extended `tools/test-jarvis-mute-contract.py` with S87 mute-pill coverage: HA mute-source attributes, header pill reason rendering, button semantics, and explicit guards that header click and `/unmute` preserve movie-mode and TV auto-mute signals.
- Added `tools/test-watch-kitchen-lighting.py`, a pure local suite for the kitchen lighting watcher: state compaction, event filtering, Frigate/call-service summaries, broad-vs-zone occupancy gaps, asleep-vacant diagnosis, missing Frigate bus events, high-brightness command loss, later low cascade-overwrite diagnosis, and sampled brightness-drop reports.
- Extended `tools/test-run-production-qa.py` with an autonomous Stage 4 browser-smoke contract: `qa-browser-smoke.js` must exit green without a prestarted dev server, emit the self-hosted loopback URL, write `stage4-browser-smoke.json`, and keep every structured smoke check passing.
- Registered `tools/qa-browser-smoke.js` as a first-class quick suite, so the autonomous app-shell/Atlas UI smoke appears directly in aggregate coverage rather than only as nested production-QA meta-test behavior.
- Extended `tools/qa-browser-smoke.js` to parse the `index.html` boot-loader file array and fetch critical cache-busted boot modules (`home-app.jsx`, `simulation.jsx`, `home-intelligence.jsx`, `home-mount.jsx`) through the self-hosted app shell, proving they return HTTP 200, JavaScript content type, and non-empty source.
- Extended `tools/test-qa-feature-recipes.py` with a screenshot drive-action schema contract: each recipe step must use a known runner action, carry the required fields, keep waits bounded, keep typed commands slash-scoped and single-line, keep screenshot artifacts inside the artifact directory, and limit synthetic API activity to the explicit Lab activity recipe.
- Extended `tools/run-stack-actions-tests.js` with trailing-slash supervisor URL coverage for stack start, service logs, and log-stream URL construction.
- Added `tools/run-bootstrap-tests.js`, a pure local suite for the `index.html` boot loader and `home-mount.jsx` contracts: boot-chain inventory, cache-busted ordered fetch/transform/execute flow, hard loader failures, module dependency order, mount-watchdog retry behavior, and registry inventory extraction.
- Added `tools/watch-ai-stack-status.py`, a read-only AI stack/supervisor watcher that polls only `/healthz` and `/api/stack/status`, reports supervisor/token/vLLM/model/in-flight state, names expected UI recovery controls, and records that no mutating supervisor endpoints were called.
- Added and registered `tools/test-watch-ai-stack-status.py`, a pure local suite for token handling, supervisor reachability, auth/rate/status failures, vLLM missing, model mismatch, ready status, read-only reports, and token-safety.
- Registered these suites in `tools/run-test-coverage.py`, bringing the current quick/unit suite count to 70.

Residual risks:

- Structured blocker/risk ledger: `docs/qa/home-app-rc-blockers.json`.
- Current structured blockers and risks:
  - `RCB-001`: Live/device validation is intentionally out of scope for the local pass.
  - `RCB-002`: Local-partial scenarios still need live/non-production scoring.
  - `RCR-003`: Windows MSI bundle built; real install smoke still deferred.
  - `RCB-004`: Frigate person detection/fade-on still needs live diagnosis.
  - `RCR-005`: Rust coverage is currently limited to the thin Tauri shell.
- Windows MSI bundle build and administrative extraction passed in an isolated target directory. A clean-machine or VM install plus first-launch smoke is still deferred so this local pass does not modify the workstation installation state.
- Live/device scenarios are not verified in this pass by design. They need a non-production HA/Frigate/media/lights environment or explicit manual approval before execution.
- `tools/scenario-evidence-map.json` currently identifies 0 documented scenarios as `live_required` with no sufficient local substitute. The previously live-only planning/abstract scenarios DOC-S58 through DOC-S68 now have local-partial contract evidence, but still need live/non-production agent scoring and HA state verification before they can be called end-to-end green.
- The new Frigate/HA watcher is read-only, but live captures from it are not counted as release-candidate evidence unless run against a non-production Frigate/Home Assistant environment.
- Existing local watcher reports from 2026-06-23 show Frigate/HA delivered no person-zone events in some windows, including a living-room watch that saw chair/potted-plant events but no `person` while camera stats looked live (`camera_fps=10`, `process_fps=10`, `detection_fps=12.7`, detection enabled). The captured failure points more toward Frigate model/filter/camera-view conditions than HA mirror/classifier/cascade logic, but fade-on still needs live Frigate detector/camera/filter diagnosis outside this local-only pass.
- Rust unit coverage is currently limited to Tauri shell/bootstrap contracts. That matches the current thin shell, but future non-trivial Tauri commands, persisted Rust state, or device-affecting Rust logic should add command-level Rust tests.

Current RC status:

- Not release-candidate ready yet within the full requested scope.
- Local non-production quick gates are green in 3 consecutive runs after the harness, scenario evidence map, S21 Jarvis mute contract, S23 CORS contract, workflow-scenario contracts, QA feature recipe contracts, feature-probe local-executable guards, Living Lights state/flag inventory guards, RC blocker ledger, chat-feed event helpers, metrics tray, metrics drawer state persistence, Simulation Mode header controls, InputRow button sweep contracts, light control-card sweep contracts, Vision card click/dwell/hover contracts, People overlay interaction/focus contracts, proactive status-row action contracts, AI Stack card recovery/S85 verb-matrix UI, Lab diagnostics/S86 button-filter contracts, mute-pill/S87 reason and auto-signal contracts, kitchen lighting watcher, stack-control, SSE, model-recovery, Frigate occupancy-path, HA-client reconnect/close-state handling, bridge/vision stale-health clearing, Frigate stale-telemetry clearing, HA-derived telemetry disconnect clearing, bridge room-context stale clearing/rendering/simulation coverage, metrics-sidecar stale model/resource clearing, `/lights` drawer offline-control, inline control-card, slash-command, capability-chip/probe-matrix, frontend overview inventory, proactive coordinator, external-reasoning, `/look`, video-labeler data-layer, Tauri runtime-glue, Tauri Rust bootstrap, S2S voice bridge, Home Intelligence helper, Vision card `/describe_clip` contract, `/apartment` data-layer, HomeApp Frigate occupancy-label, People overlay identity-mutation refresh, Simulation Mode control-store, Simulation Mode command/containment, Simulation Mode camera, Simulation Mode scenario-fixture, Living Lights recent-override/session-intent, Stage 4 autonomous UI smoke, and bootstrap/load-order coverage changes.
- Remaining work: run local-partial scenarios only in a non-production/live environment with explicit approval, or add deeper simulation/mock-backed automation where it can prove actual behavior without touching real devices.
