---
title: Add the atomic parent-relationship authority foundation
target: backend
type: added
---

Add owner-only E5d persistence for a private, single-use confirmation that
promotes exactly two reviewed legacy `parent` labels into two explicit
`parent_of` facts. The schema reuses the existing People UUIDs, binds every
edge and future fact axis to one 15-minute proposal, and reserves normalized
content-free authority receipts for the all-or-nothing commit. No runtime role
receives access and production remains pinned to revision 0006a in record-only
mode.
