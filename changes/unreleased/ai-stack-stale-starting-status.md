---
title: Ignore stale AI stack start tasks once services are ready
target: web
type: fixed
---

The AI stack card now treats recovered start tasks as stale when all services and the expected model are already ready, so it no longer shows misleading "starting" or "in progress" status while the model is responding.
